<?php

namespace App\Http\Controllers;

// Legacy shim: the real controller is EmpleadoController. Keep an empty class
// named Empleado to avoid autoloading issues if any legacy code refers to it.
class Empleado
{
    // intentionally empty
}
